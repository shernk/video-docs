

import { HttpUtilityService } from "./http-utility.service";
describe("Http Service", () => {
  it("Exists", () => {
    // assert
    expect(HttpUtilityService).toBeDefined();
  });

  it("Can be built", () => {
    // arrange

    // act
  });
});
